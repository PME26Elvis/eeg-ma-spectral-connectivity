# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.env

# Raw / intermediate EEG: never commit
/data/raw/*
!/data/raw/README.md
/data/cache/*
!/data/cache/.gitkeep

# Canonical analysis results and report figures are intentionally versioned.
# Keep only explicitly temporary/scratch outputs ignored.
/results/scratch/
/results/tmp/

# Feature tables are intentionally NOT ignored: they are small enough to version.

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
